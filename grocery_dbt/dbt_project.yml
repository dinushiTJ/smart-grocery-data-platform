name: grocery_dbt
version: '1.0.0'
config-version: 2

profile: grocery_dbt

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

models:
  grocery_dbt:
    +materialized: view
